enum HomeStates {
  HomeState,
  ActivityState,
  ReferenceDocumentState,
  ListState,
  StaffState,
  TaskState,
  IssueState,
}
enum States { Loading, Completed, Error }
enum UserRole { User, Coordinator, Admin }
