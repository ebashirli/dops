import 'package:flutter/material.dart';

class TaskFollowingView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Task Following View',
            style: TextStyle(
              fontWeight: FontWeight.bold,
              fontSize: 20,
            )));
  }
}
